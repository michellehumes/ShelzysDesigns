{%- comment -%}
  Shelzy's Designs - Trust Strip

  Final trust section for bottom of homepage with:
  - Free Shipping $75+
  - 3-5 Day Turnaround
  - Secure Checkout
  - Handcrafted in the USA

  Add to homepage above footer: {% render 'shelzys-trust-strip' %}
{%- endcomment -%}

<section class="sz-trust-strip">
  <div class="sz-trust-strip-container">

    <div class="sz-trust-strip-item">
      <div class="sz-trust-strip-icon">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <rect x="1" y="3" width="15" height="13"/>
          <polygon points="16 8 20 8 23 11 23 16 16 16 16 8"/>
          <circle cx="5.5" cy="18.5" r="2.5"/>
          <circle cx="18.5" cy="18.5" r="2.5"/>
        </svg>
      </div>
      <div class="sz-trust-strip-text">
        <span class="sz-trust-strip-title">Free Shipping</span>
        <span class="sz-trust-strip-subtitle">On orders $75+</span>
      </div>
    </div>

    <div class="sz-trust-strip-item">
      <div class="sz-trust-strip-icon">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <circle cx="12" cy="12" r="10"/>
          <polyline points="12 6 12 12 16 14"/>
        </svg>
      </div>
      <div class="sz-trust-strip-text">
        <span class="sz-trust-strip-title">3-5 Day Turnaround</span>
        <span class="sz-trust-strip-subtitle">Rush options available</span>
      </div>
    </div>

    <div class="sz-trust-strip-item">
      <div class="sz-trust-strip-icon">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
          <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
        </svg>
      </div>
      <div class="sz-trust-strip-text">
        <span class="sz-trust-strip-title">Secure Checkout</span>
        <span class="sz-trust-strip-subtitle">256-bit SSL encryption</span>
      </div>
    </div>

    <div class="sz-trust-strip-item">
      <div class="sz-trust-strip-icon">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
        </svg>
      </div>
      <div class="sz-trust-strip-text">
        <span class="sz-trust-strip-title">Handcrafted in USA</span>
        <span class="sz-trust-strip-subtitle">Made with care</span>
      </div>
    </div>

  </div>
</section>

<style>
  .sz-trust-strip {
    background: #FAF8F5;
    padding: 60px 20px;
    border-top: 1px solid #E8E4DE;
    border-bottom: 1px solid #E8E4DE;
  }

  @media (max-width: 768px) {
    .sz-trust-strip {
      padding: 48px 16px;
    }
  }

  .sz-trust-strip-container {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 32px;
  }

  @media (max-width: 900px) {
    .sz-trust-strip-container {
      grid-template-columns: repeat(2, 1fr);
      gap: 24px;
    }
  }

  @media (max-width: 500px) {
    .sz-trust-strip-container {
      grid-template-columns: 1fr;
      gap: 20px;
    }
  }

  .sz-trust-strip-item {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 20px;
    background: #FFFFFF;
    border: 1px solid #E8E4DE;
  }

  @media (max-width: 500px) {
    .sz-trust-strip-item {
      padding: 16px;
    }
  }

  .sz-trust-strip-icon {
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #fb5887 0%, #fb5887 100%);
    border-radius: 12px;
    color: #FFFFFF;
    flex-shrink: 0;
  }

  @media (max-width: 768px) {
    .sz-trust-strip-icon {
      width: 48px;
      height: 48px;
    }

    .sz-trust-strip-icon svg {
      width: 22px;
      height: 22px;
    }
  }

  .sz-trust-strip-text {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .sz-trust-strip-title {
    font-family: "Inter", sans-serif;
    font-size: 15px;
    font-weight: 600;
    color: #1F1F1F;
  }

  .sz-trust-strip-subtitle {
    font-family: "Inter", sans-serif;
    font-size: 13px;
    color: #7A7A7A;
  }

  @media (max-width: 768px) {
    .sz-trust-strip-title {
      font-size: 14px;
    }

    .sz-trust-strip-subtitle {
      font-size: 12px;
    }
  }
</style>
