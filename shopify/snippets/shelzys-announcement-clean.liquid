{%- comment -%}
  Shelzy's Designs - Clean Announcement Bar

  Single, consistent message across all pages
  Add to theme: {% render 'shelzys-announcement-clean' %}
{%- endcomment -%}

<div class="sz-announcement-clean" id="szAnnouncementBar">
  <div class="sz-announcement-inner">
    <p>
      <span class="sz-announcement-item">Free shipping $75+</span>
      <span class="sz-announcement-sep">•</span>
      <span class="sz-announcement-item">3–5 day turnaround</span>
      <span class="sz-announcement-sep">•</span>
      <span class="sz-announcement-item">Permanent sublimation printing</span>
    </p>
  </div>
</div>

<style>
  .sz-announcement-clean {
    background: #2C3E2D;
    padding: 12px 20px;
    text-align: center;
  }

  .sz-announcement-inner {
    max-width: 1200px;
    margin: 0 auto;
  }

  .sz-announcement-clean p {
    font-family: "Inter", -apple-system, BlinkMacSystemFont, sans-serif;
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0.03em;
    color: #FFFFFF;
    margin: 0;
    line-height: 1.4;
  }

  .sz-announcement-sep {
    margin: 0 12px;
    opacity: 0.5;
  }

  /* Mobile: Stack items */
  @media (max-width: 600px) {
    .sz-announcement-clean p {
      font-size: 12px;
    }

    .sz-announcement-sep {
      margin: 0 8px;
    }

    /* Hide separators and show as single rotating message on very small screens */
    @media (max-width: 400px) {
      .sz-announcement-item:not(.active),
      .sz-announcement-sep {
        display: none;
      }

      .sz-announcement-item.active {
        display: inline;
      }
    }
  }
</style>

<script>
(function() {
  // Simple rotation for very small screens
  if (window.innerWidth <= 400) {
    var items = document.querySelectorAll('.sz-announcement-item');
    if (items.length === 0) return;

    var currentIndex = 0;
    items[0].classList.add('active');

    setInterval(function() {
      items[currentIndex].classList.remove('active');
      currentIndex = (currentIndex + 1) % items.length;
      items[currentIndex].classList.add('active');
    }, 3000);
  }
})();
</script>
