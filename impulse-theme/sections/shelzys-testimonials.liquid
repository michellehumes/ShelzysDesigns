{%- comment -%}
  SHELZY'S DESIGNS: Customer Love / Testimonials Section for Impulse Theme
  Location: sections/shelzys-testimonials.liquid
{%- endcomment -%}

<section class="shelzys-testimonials" data-section-id="{{ section.id }}" style="background-color: {{ section.settings.bg_color }};">
  <div class="page-width">
    <div class="testimonials__header text-center">
      <h2 class="testimonials__title h2">{{ section.settings.title }}</h2>
      {%- if section.settings.subtitle != blank -%}
        <p class="testimonials__subtitle">{{ section.settings.subtitle }}</p>
      {%- endif -%}
    </div>

    <div class="testimonials__grid">
      {%- for block in section.blocks -%}
        <div class="testimonial-card" {{ block.shopify_attributes }}>
          {%- if section.settings.show_stars -%}
            <div class="testimonial-card__stars">
              {%- for i in (1..block.settings.stars) -%}★{%- endfor -%}
            </div>
          {%- endif -%}

          <blockquote class="testimonial-card__quote">
            "{{ block.settings.quote }}"
          </blockquote>

          <div class="testimonial-card__author">
            {%- if block.settings.image != blank -%}
              <div class="testimonial-card__avatar">
                <img src="{{ block.settings.image | image_url: width: 100 }}" alt="{{ block.settings.name }}" loading="lazy">
              </div>
            {%- endif -%}
            <div class="testimonial-card__info">
              <span class="testimonial-card__name">{{ block.settings.name }}</span>
              {%- if block.settings.meta != blank -%}
                <span class="testimonial-card__meta">{{ block.settings.meta }}</span>
              {%- endif -%}
            </div>
          </div>

          {%- if block.settings.product != blank -%}
            {%- assign linked_product = all_products[block.settings.product] -%}
            {%- if linked_product -%}
              <a href="{{ linked_product.url }}" class="testimonial-card__product">
                {%- if linked_product.featured_image -%}
                  <img src="{{ linked_product.featured_image | image_url: width: 80 }}" alt="{{ linked_product.title }}" class="testimonial-card__product-image">
                {%- endif -%}
                <span class="testimonial-card__product-title">{{ linked_product.title | truncate: 25 }}</span>
              </a>
            {%- endif -%}
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

<style>
.shelzys-testimonials {
  padding: 80px 0;
}

.testimonials__header {
  margin-bottom: 48px;
}

.testimonials__title {
  font-size: clamp(28px, 4vw, 36px);
  font-weight: 700;
  margin: 0 0 12px;
  color: #1a1a1a;
}

.testimonials__subtitle {
  font-size: 16px;
  color: #666;
  margin: 0;
}

.testimonials__grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
}

.testimonial-card {
  background: #fff;
  border-radius: 12px;
  padding: 32px;
  box-shadow: 0 4px 20px rgba(0,0,0,0.06);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.testimonial-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 32px rgba(0,0,0,0.1);
}

.testimonial-card__stars {
  color: #FFB800;
  font-size: 16px;
  letter-spacing: 2px;
  margin-bottom: 16px;
}

.testimonial-card__quote {
  font-size: 16px;
  line-height: 1.65;
  color: #333;
  margin: 0 0 24px;
  font-style: italic;
}

.testimonial-card__author {
  display: flex;
  align-items: center;
  gap: 12px;
}

.testimonial-card__avatar {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  overflow: hidden;
  flex-shrink: 0;
}

.testimonial-card__avatar img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.testimonial-card__info {
  display: flex;
  flex-direction: column;
}

.testimonial-card__name {
  font-size: 15px;
  font-weight: 600;
  color: #1a1a1a;
}

.testimonial-card__meta {
  font-size: 13px;
  color: #888;
}

.testimonial-card__product {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-top: 20px;
  padding-top: 20px;
  border-top: 1px solid #f0f0f0;
  text-decoration: none;
  color: inherit;
}

.testimonial-card__product-image {
  width: 50px;
  height: 50px;
  object-fit: cover;
  border-radius: 6px;
}

.testimonial-card__product-title {
  font-size: 13px;
  color: var(--color-sage, #8B9D77);
  font-weight: 500;
}

@media (max-width: 989px) {
  .testimonials__grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }
}

@media (max-width: 649px) {
  .shelzys-testimonials {
    padding: 56px 0;
  }

  .testimonials__grid {
    grid-template-columns: 1fr;
    gap: 20px;
  }

  .testimonial-card {
    padding: 24px;
  }
}
</style>

{% schema %}
{
  "name": "Shelzys Testimonials",
  "tag": "section",
  "class": "section-testimonials",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Loved by Brides, Teams & Gift-Givers"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle"
    },
    {
      "type": "checkbox",
      "id": "show_stars",
      "label": "Show star ratings",
      "default": true
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background color",
      "default": "#F8F8F8"
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        {
          "type": "range",
          "id": "stars",
          "label": "Star rating",
          "min": 1,
          "max": 5,
          "step": 1,
          "default": 5
        },
        {
          "type": "textarea",
          "id": "quote",
          "label": "Quote",
          "default": "Customer testimonial quote goes here."
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Customer photo (optional)"
        },
        {
          "type": "text",
          "id": "name",
          "label": "Customer name",
          "default": "Customer Name"
        },
        {
          "type": "text",
          "id": "meta",
          "label": "Meta info",
          "default": "Bride, 2024 Wedding"
        },
        {
          "type": "product",
          "id": "product",
          "label": "Link to product (optional)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Shelzys Testimonials",
      "blocks": [
        {
          "type": "testimonial",
          "settings": {
            "stars": 5,
            "quote": "Exactly like the photos — the quality is insane. My girls screamed when they saw them!",
            "name": "Kayla M.",
            "meta": "Bride, 2024 Wedding"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "stars": 5,
            "quote": "We ordered 40 bottles with our logo for a company retreat. Everyone still uses them daily.",
            "name": "David R.",
            "meta": "Corporate Event Client"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "stars": 5,
            "quote": "These are my go-to gift now. So much more thoughtful than a generic bottle.",
            "name": "Melissa T.",
            "meta": "Repeat Customer"
          }
        }
      ]
    }
  ]
}
{% endschema %}
