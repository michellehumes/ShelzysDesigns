{%- comment -%}
  Shelzy's Designs - Enhanced Navigation Header

  Features:
  - Logo left, cart right
  - Simplified nav: 3 categories only (Custom Water Bottles, Kids, Holiday)
  - Mobile hamburger menu

  Add to theme.liquid after announcement bar or replace header section:
  {% render 'shelzys-nav-header' %}
{%- endcomment -%}

<header class="sz-header" id="szHeader">
  <div class="sz-header-inner">

    {%- comment -%} Mobile Menu Toggle {%- endcomment -%}
    <button class="sz-menu-toggle" id="szMenuToggle" aria-label="Open menu" aria-expanded="false">
      <span class="sz-hamburger">
        <span></span>
        <span></span>
        <span></span>
      </span>
    </button>

    {%- comment -%} Logo {%- endcomment -%}
    <div class="sz-header-logo">
      <a href="/" class="sz-logo-link">
        {%- if settings.logo != blank -%}
          <img src="{{ settings.logo | image_url: width: 300 }}" alt="{{ shop.name }}" class="sz-logo-img">
        {%- else -%}
          <span class="sz-logo-text">{{ shop.name }}</span>
        {%- endif -%}
      </a>
    </div>

    {%- comment -%} Main Navigation - 3 Categories Only {%- endcomment -%}
    <nav class="sz-main-nav" id="szMainNav" aria-label="Main navigation">
      <ul class="sz-nav-list">

        {%- comment -%} Custom Water Bottles - Primary Category {%- endcomment -%}
        <li class="sz-nav-item">
          <a href="/collections/custom-water-bottles" class="sz-nav-link sz-nav-featured">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" style="margin-right: 4px;">
              <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
            </svg>
            Custom Water Bottles
          </a>
        </li>

        {%- comment -%} Kids Custom Water Bottles {%- endcomment -%}
        <li class="sz-nav-item">
          <a href="/collections/kids" class="sz-nav-link">
            Kids Custom Water Bottles
          </a>
        </li>

        {%- comment -%} Holiday Water Bottles {%- endcomment -%}
        <li class="sz-nav-item">
          <a href="/collections/holiday" class="sz-nav-link">
            Holiday Water Bottles
          </a>
        </li>

        {%- comment -%} FAQ {%- endcomment -%}
        <li class="sz-nav-item">
          <a href="/pages/faq" class="sz-nav-link">FAQ</a>
        </li>

        {%- comment -%} Contact {%- endcomment -%}
        <li class="sz-nav-item">
          <a href="/pages/contact" class="sz-nav-link">Contact</a>
        </li>

      </ul>
    </nav>

    {%- comment -%} Header Actions (Search, Account, Cart) {%- endcomment -%}
    <div class="sz-header-actions">
      <a href="/search" class="sz-header-icon" aria-label="Search">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/>
        </svg>
      </a>

      <a href="/account" class="sz-header-icon sz-hide-mobile" aria-label="Account">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>
        </svg>
      </a>

      <a href="/cart" class="sz-header-icon sz-cart-icon" aria-label="Cart">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 0 1-8 0"/>
        </svg>
        <span class="sz-cart-count" id="szCartCount">{{ cart.item_count }}</span>
      </a>
    </div>

  </div>
</header>

{%- comment -%} Mobile Menu Overlay {%- endcomment -%}
<div class="sz-mobile-menu" id="szMobileMenu" aria-hidden="true">
  <div class="sz-mobile-menu-inner">
    <nav class="sz-mobile-nav" aria-label="Mobile navigation">

      {%- comment -%} Custom Water Bottles {%- endcomment -%}
      <a href="/collections/custom-water-bottles" class="sz-mobile-link sz-mobile-featured">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" style="margin-right: 6px;">
          <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
        </svg>
        Custom Water Bottles
      </a>

      {%- comment -%} Kids Custom Water Bottles {%- endcomment -%}
      <a href="/collections/kids" class="sz-mobile-link">
        Kids Custom Water Bottles
      </a>

      {%- comment -%} Holiday Water Bottles {%- endcomment -%}
      <a href="/collections/holiday" class="sz-mobile-link">
        Holiday Water Bottles
      </a>

      {%- comment -%} Utility Links {%- endcomment -%}
      <a href="/pages/faq" class="sz-mobile-link">FAQ</a>
      <a href="/pages/about" class="sz-mobile-link">About</a>
      <a href="/pages/contact" class="sz-mobile-link">Contact</a>
    </nav>
  </div>
</div>

<style>
  /* ═══════════════════════════════════════════════════════════════════
     HEADER CONTAINER
     ═══════════════════════════════════════════════════════════════════ */
  .sz-header {
    background: #FFFFFF;
    border-bottom: 1px solid #E8E4DE;
    position: sticky;
    top: 0;
    z-index: 1000;
  }

  .sz-header-inner {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 24px;
    gap: 32px;
  }

  @media (max-width: 1024px) {
    .sz-header-inner {
      padding: 12px 16px;
      gap: 16px;
    }
  }

  /* ═══════════════════════════════════════════════════════════════════
     LOGO
     ═══════════════════════════════════════════════════════════════════ */
  .sz-header-logo {
    flex-shrink: 0;
  }

  .sz-logo-link {
    display: block;
  }

  .sz-logo-img {
    height: 44px;
    width: auto;
    display: block;
  }

  .sz-logo-text {
    font-family: "Montserrat", sans-serif;
    font-size: 24px;
    font-weight: 500;
    color: #1F1F1F;
  }

  @media (max-width: 768px) {
    .sz-logo-img {
      height: 36px;
    }
  }

  /* ═══════════════════════════════════════════════════════════════════
     MAIN NAVIGATION (Desktop)
     ═══════════════════════════════════════════════════════════════════ */
  .sz-main-nav {
    flex: 1;
    display: flex;
    justify-content: center;
  }

  @media (max-width: 1024px) {
    .sz-main-nav {
      display: none;
    }
  }

  .sz-nav-list {
    display: flex;
    align-items: center;
    gap: 8px;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .sz-nav-item {
    position: relative;
  }

  .sz-nav-link {
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 10px 16px;
    font-family: "Inter", sans-serif;
    font-size: 14px;
    font-weight: 500;
    color: #1F1F1F;
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .sz-nav-link:hover {
    color: #1a1a1a;
  }

  /* Featured Nav Link (Custom Water Bottles) */
  .sz-nav-featured {
    color: #1a1a1a !important;
    font-weight: 600 !important;
  }

  .sz-nav-featured svg {
    color: #D4AF37;
  }

  /* ═══════════════════════════════════════════════════════════════════
     HEADER ACTIONS
     ═══════════════════════════════════════════════════════════════════ */
  .sz-header-actions {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .sz-header-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    color: #1F1F1F;
    transition: color 0.2s ease;
  }

  .sz-header-icon:hover {
    color: #1a1a1a;
  }

  .sz-cart-icon {
    position: relative;
  }

  .sz-cart-count {
    position: absolute;
    top: 2px;
    right: 2px;
    min-width: 18px;
    height: 18px;
    background: #2C3E2D;
    color: #FFFFFF;
    font-family: "Inter", sans-serif;
    font-size: 11px;
    font-weight: 600;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .sz-cart-count:empty,
  .sz-cart-count[data-count="0"] {
    display: none;
  }

  .sz-hide-mobile {
    display: flex;
  }

  @media (max-width: 768px) {
    .sz-hide-mobile {
      display: none;
    }
  }

  /* ═══════════════════════════════════════════════════════════════════
     MOBILE MENU TOGGLE
     ═══════════════════════════════════════════════════════════════════ */
  .sz-menu-toggle {
    display: none;
    width: 40px;
    height: 40px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    order: -1;
  }

  @media (max-width: 1024px) {
    .sz-menu-toggle {
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }

  .sz-hamburger {
    display: flex;
    flex-direction: column;
    gap: 5px;
    width: 22px;
  }

  .sz-hamburger span {
    height: 2px;
    background: #1F1F1F;
    transition: all 0.3s ease;
  }

  .sz-menu-toggle[aria-expanded="true"] .sz-hamburger span:nth-child(1) {
    transform: rotate(45deg) translateY(10px);
  }

  .sz-menu-toggle[aria-expanded="true"] .sz-hamburger span:nth-child(2) {
    opacity: 0;
  }

  .sz-menu-toggle[aria-expanded="true"] .sz-hamburger span:nth-child(3) {
    transform: rotate(-45deg) translateY(-10px);
  }

  /* ═══════════════════════════════════════════════════════════════════
     MOBILE MENU OVERLAY
     ═══════════════════════════════════════════════════════════════════ */
  .sz-mobile-menu {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: #FFFFFF;
    z-index: 999;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    padding-top: 70px;
    overflow-y: auto;
  }

  .sz-mobile-menu.sz-open {
    opacity: 1;
    visibility: visible;
  }

  .sz-mobile-menu-inner {
    padding: 24px;
    max-width: 400px;
  }

  .sz-mobile-nav {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .sz-mobile-link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 0;
    font-family: "Inter", sans-serif;
    font-size: 18px;
    font-weight: 500;
    color: #1F1F1F;
    text-decoration: none;
    border-bottom: 1px solid #E8E4DE;
    background: none;
    border-top: none;
    border-left: none;
    border-right: none;
    cursor: pointer;
    width: 100%;
    text-align: left;
  }

  .sz-mobile-link:hover {
    color: #1a1a1a;
  }

  /* Mobile Featured Link */
  .sz-mobile-featured {
    color: #1a1a1a !important;
    font-weight: 600 !important;
    background: linear-gradient(90deg, rgba(78, 95, 74, 0.08) 0%, transparent 100%);
  }

  .sz-mobile-featured svg {
    color: #D4AF37;
  }
</style>

<script>
(function() {
  var menuToggle = document.getElementById('szMenuToggle');
  var mobileMenu = document.getElementById('szMobileMenu');

  function openMenu() {
    menuToggle.setAttribute('aria-expanded', 'true');
    mobileMenu.classList.add('sz-open');
    mobileMenu.setAttribute('aria-hidden', 'false');
    document.body.style.overflow = 'hidden';
  }

  function closeMenu() {
    menuToggle.setAttribute('aria-expanded', 'false');
    mobileMenu.classList.remove('sz-open');
    mobileMenu.setAttribute('aria-hidden', 'true');
    document.body.style.overflow = '';
    menuToggle.focus();
  }

  // Toggle mobile menu
  menuToggle.addEventListener('click', function() {
    var expanded = this.getAttribute('aria-expanded') === 'true';
    if (expanded) {
      closeMenu();
    } else {
      openMenu();
    }
  });

  // Keyboard navigation
  document.addEventListener('keydown', function(e) {
    if (!mobileMenu.classList.contains('sz-open')) return;
    if (e.key === 'Escape') {
      e.preventDefault();
      closeMenu();
    }
  });

  // Close menu when clicking outside
  mobileMenu.addEventListener('click', function(e) {
    if (e.target === mobileMenu) {
      closeMenu();
    }
  });
})();
</script>
