{%- comment -%}
  Shelzy's Designs - Shop by Category Grid

  3 category cards with images, titles, and descriptions:
  1. Custom Water Bottles
  2. Kids Custom Water Bottles
  3. Holiday Water Bottles

  Add to homepage: {% render 'shelzys-occasion-grid' %}
{%- endcomment -%}

<section class="sz-category-grid" id="shop-by-category">
  <div class="sz-category-container">

    <header class="sz-section-header">
      <span class="sz-eyebrow">Find Your Perfect Bottle</span>
      <h2>Shop by Category</h2>
    </header>

    <div class="sz-category-cards">

      {%- comment -%} 1. Custom Water Bottles {%- endcomment -%}
      <a href="/collections/custom-water-bottles" class="sz-category-card sz-card-featured">
        <div class="sz-category-card-image">
          {%- assign custom_col = collections['custom-water-bottles'] | default: collections.all -%}
          {%- if custom_col.image -%}
            <img src="{{ custom_col.image | img_url: '600x800', crop: 'center' }}" alt="Custom water bottles" loading="lazy">
          {%- else -%}
            <div class="sz-category-placeholder" style="background: linear-gradient(135deg, #B8C5B0 0%, #fb5887 100%);"></div>
          {%- endif -%}
          <div class="sz-category-card-overlay"></div>
        </div>
        <div class="sz-category-card-content">
          <h3>Custom Water Bottles</h3>
          <p>Beautiful personalized bottles with permanent sublimation printing. No vinyl, no peeling — ever.</p>
          <span class="sz-category-cta">Shop Now →</span>
        </div>
      </a>

      {%- comment -%} 2. Kids Custom Water Bottles {%- endcomment -%}
      <a href="/collections/kids" class="sz-category-card">
        <div class="sz-category-card-image">
          {%- assign kids_col = collections['kids'] -%}
          {%- if kids_col.image -%}
            <img src="{{ kids_col.image | img_url: '600x800', crop: 'center' }}" alt="Kids custom water bottles" loading="lazy">
          {%- else -%}
            <div class="sz-category-placeholder" style="background: linear-gradient(135deg, #7CB9E8 0%, #4A90D9 100%);"></div>
          {%- endif -%}
          <div class="sz-category-card-overlay"></div>
        </div>
        <div class="sz-category-card-content">
          <h3>Kids Custom Water Bottles</h3>
          <p>Durable bottles for kids, sports teams, dance teams, and everyday adventures.</p>
          <span class="sz-category-cta">Shop Kids →</span>
        </div>
      </a>

      {%- comment -%} 3. Holiday Water Bottles {%- endcomment -%}
      <a href="/collections/holiday" class="sz-category-card">
        <div class="sz-category-card-image">
          {%- assign holiday_col = collections['holiday'] -%}
          {%- if holiday_col.image -%}
            <img src="{{ holiday_col.image | img_url: '600x800', crop: 'center' }}" alt="Holiday water bottles" loading="lazy">
          {%- else -%}
            <div class="sz-category-placeholder" style="background: linear-gradient(135deg, #C45B5B 0%, #8B3A3A 100%);"></div>
          {%- endif -%}
          <div class="sz-category-card-overlay"></div>
        </div>
        <div class="sz-category-card-content">
          <h3>Holiday Water Bottles</h3>
          <p>Festive designs for Christmas, holidays, and seasonal celebrations. Perfect gifts!</p>
          <span class="sz-category-cta">Shop Holiday →</span>
        </div>
      </a>

    </div>

  </div>
</section>

<style>
  .sz-category-grid {
    padding: 100px 20px;
    background: #FFFFFF;
  }

  @media (max-width: 768px) {
    .sz-category-grid {
      padding: 60px 16px;
    }
  }

  .sz-category-container {
    max-width: 1400px;
    margin: 0 auto;
  }

  .sz-category-grid .sz-section-header {
    text-align: center;
    margin-bottom: 60px;
  }

  .sz-category-grid .sz-eyebrow {
    display: block;
    font-family: "Montserrat", sans-serif;
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: #B5A48A;
    margin-bottom: 12px;
  }

  .sz-category-grid h2 {
    font-family: "Montserrat", sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 500;
    color: #1F1F1F;
    margin: 0;
  }

  /* ─── Card Grid (3 cards) ─── */
  .sz-category-cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  @media (max-width: 1024px) {
    .sz-category-cards {
      grid-template-columns: 1fr;
      max-width: 600px;
      margin: 0 auto;
      gap: 20px;
    }
  }

  /* ─── Individual Cards ─── */
  .sz-category-card {
    position: relative;
    display: block;
    text-decoration: none;
    overflow: hidden;
    aspect-ratio: 3/4;
    background: #FAF8F5;
  }

  @media (max-width: 1024px) {
    .sz-category-card {
      aspect-ratio: 16/9;
      min-height: 280px;
    }
  }

  .sz-category-card-image {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
  }

  .sz-category-card-image img,
  .sz-category-placeholder {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .sz-category-card:hover .sz-category-card-image img,
  .sz-category-card:hover .sz-category-placeholder {
    transform: scale(1.06);
  }

  .sz-category-card-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      180deg,
      rgba(31, 31, 31, 0) 30%,
      rgba(31, 31, 31, 0.7) 100%
    );
    transition: background 0.4s ease;
  }

  .sz-category-card:hover .sz-category-card-overlay {
    background: linear-gradient(
      180deg,
      rgba(31, 31, 31, 0.1) 0%,
      rgba(31, 31, 31, 0.8) 100%
    );
  }

  /* ─── Card Content ─── */
  .sz-category-card-content {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 32px;
    color: #FFFFFF;
    z-index: 2;
  }

  @media (max-width: 600px) {
    .sz-category-card-content {
      padding: 24px;
    }
  }

  .sz-category-card-content h3 {
    font-family: "Montserrat", sans-serif;
    font-size: 26px;
    font-weight: 500;
    margin: 0 0 8px 0;
    line-height: 1.2;
  }

  @media (max-width: 768px) {
    .sz-category-card-content h3 {
      font-size: 22px;
    }
  }

  .sz-category-card-content p {
    font-family: "Inter", sans-serif;
    font-size: 14px;
    font-weight: 400;
    line-height: 1.5;
    margin: 0 0 16px 0;
    opacity: 0.9;
    max-width: 280px;
  }

  @media (max-width: 600px) {
    .sz-category-card-content p {
      font-size: 13px;
      margin-bottom: 12px;
    }
  }

  .sz-category-cta {
    display: inline-block;
    font-family: "Inter", sans-serif;
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.04em;
    opacity: 0;
    transform: translateY(8px);
    transition: all 0.3s ease;
  }

  .sz-category-card:hover .sz-category-cta {
    opacity: 1;
    transform: translateY(0);
  }

  /* Mobile: always show CTA */
  @media (max-width: 768px) {
    .sz-category-cta {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Featured card styling */
  .sz-card-featured .sz-category-card-content h3::before {
    content: '★ ';
    color: #D4AF37;
  }
</style>
