{%- comment -%}
  Shelzy's Designs - Stunning Hero Banner
  A visually striking hero with parallax, gradient overlays, and animated elements
{%- endcomment -%}

<section class="sz-hero-banner" style="--hero-height: {{ section.settings.height }}vh; --mobile-height: {{ section.settings.mobile_height }}vh;">
  {%- if section.settings.background_image != blank -%}
    <div class="sz-hero-media">
      <img
        src="{{ section.settings.background_image | image_url: width: 2000 }}"
        srcset="{{ section.settings.background_image | image_url: width: 800 }} 800w,
                {{ section.settings.background_image | image_url: width: 1200 }} 1200w,
                {{ section.settings.background_image | image_url: width: 1600 }} 1600w,
                {{ section.settings.background_image | image_url: width: 2000 }} 2000w"
        sizes="100vw"
        alt="{{ section.settings.background_image.alt | default: shop.name }}"
        class="sz-hero-image"
        loading="eager"
        fetchpriority="high"
      >
    </div>
  {%- else -%}
    <div class="sz-hero-media">
      <img
        src="{{ 'hero-bridal-party-5.webp' | asset_url }}"
        alt="{{ shop.name }} - Personalized Bottles for Life's Special Moments"
        class="sz-hero-image"
        loading="eager"
        fetchpriority="high"
      >
    </div>
  {%- endif -%}

  <div class="sz-hero-overlay" style="--overlay-opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};"></div>

  <div class="sz-hero-content">
    <div class="sz-hero-content-inner {{ section.settings.content_position }}">
      {%- if section.settings.badge_text != blank -%}
        <span class="sz-hero-badge animate-fade-up">
          {%- if section.settings.badge_icon == 'star' -%}
            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
          {%- elsif section.settings.badge_icon == 'heart' -%}
            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>
          {%- elsif section.settings.badge_icon == 'sparkle' -%}
            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M12 3l1.5 6.5L20 12l-6.5 1.5L12 20l-1.5-6.5L4 12l6.5-1.5L12 3z"/></svg>
          {%- endif -%}
          {{ section.settings.badge_text }}
        </span>
      {%- endif -%}

      {%- if section.settings.heading != blank -%}
        <h1 class="sz-hero-heading animate-fade-up delay-1">{{ section.settings.heading }}</h1>
      {%- endif -%}

      {%- if section.settings.subheading != blank -%}
        <p class="sz-hero-subheading animate-fade-up delay-2">{{ section.settings.subheading }}</p>
      {%- endif -%}

      <div class="sz-hero-buttons animate-fade-up delay-3">
        {%- if section.settings.button_label != blank -%}
          <a href="{{ section.settings.button_link }}" class="sz-hero-btn sz-hero-btn-primary">
            {{ section.settings.button_label }}
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </a>
        {%- endif -%}
        {%- if section.settings.button_label_2 != blank -%}
          <a href="{{ section.settings.button_link_2 }}" class="sz-hero-btn sz-hero-btn-secondary">
            {{ section.settings.button_label_2 }}
          </a>
        {%- endif -%}
      </div>

      {%- if section.settings.show_trust_badges -%}
        <div class="sz-hero-trust animate-fade-up delay-4">
          <div class="sz-trust-item">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path d="M9 12l2 2 4-4"/></svg>
            <span>Permanent Printing</span>
          </div>
          <div class="sz-trust-item">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="1" y="3" width="15" height="13"/><polygon points="16 8 20 8 23 11 23 16 16 16 16 8"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
            <span>Free Shipping $75+</span>
          </div>
          <div class="sz-trust-item">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>
            <span>Made with Love</span>
          </div>
        </div>
      {%- endif -%}
    </div>
  </div>

  {%- if section.settings.show_scroll_indicator -%}
    <div class="sz-hero-scroll">
      <span>Scroll</span>
      <div class="sz-scroll-line"></div>
    </div>
  {%- endif -%}
</section>

<style>
  .sz-hero-banner {
    position: relative;
    height: var(--hero-height);
    min-height: 500px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  @media (max-width: 768px) {
    .sz-hero-banner {
      height: var(--mobile-height);
      min-height: 400px;
    }
  }

  .sz-hero-media {
    position: absolute;
    inset: 0;
    z-index: 1;
  }

  .sz-hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    transform: scale(1.05);
    animation: sz-hero-zoom 20s ease-out forwards;
  }

  @keyframes sz-hero-zoom {
    to { transform: scale(1); }
  }

  .sz-hero-placeholder {
    background: linear-gradient(135deg, #2C3E2D 0%, #4E5F4A 50%, #8BAA88 100%);
  }

  .sz-hero-gradient-bg {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 20% 50%, rgba(139, 170, 136, 0.3) 0%, transparent 50%),
      radial-gradient(ellipse at 80% 50%, rgba(212, 175, 55, 0.2) 0%, transparent 50%),
      radial-gradient(ellipse at 50% 100%, rgba(78, 95, 74, 0.4) 0%, transparent 50%);
    animation: sz-gradient-shift 15s ease-in-out infinite alternate;
  }

  @keyframes sz-gradient-shift {
    0% { opacity: 1; }
    100% { opacity: 0.8; transform: scale(1.1); }
  }

  .sz-hero-overlay {
    position: absolute;
    inset: 0;
    z-index: 2;
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, calc(var(--overlay-opacity) * 0.5)) 0%,
      rgba(0, 0, 0, var(--overlay-opacity)) 50%,
      rgba(0, 0, 0, calc(var(--overlay-opacity) * 0.8)) 100%
    );
  }

  .sz-hero-content {
    position: relative;
    z-index: 3;
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 40px 24px;
  }

  .sz-hero-content-inner {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .sz-hero-content-inner.center {
    align-items: center;
    text-align: center;
  }

  .sz-hero-content-inner.left {
    align-items: flex-start;
    text-align: left;
    max-width: 650px;
  }

  .sz-hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px;
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 50px;
    font-family: "Inter", sans-serif;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: #fff;
  }

  .sz-hero-badge svg {
    color: #D4AF37;
  }

  .sz-hero-heading {
    font-family: "Playfair Display", Georgia, serif;
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 500;
    line-height: 1.1;
    color: #fff;
    margin: 0;
    text-shadow: 0 2px 20px rgba(0, 0, 0, 0.3);
  }

  .sz-hero-subheading {
    font-family: "Inter", sans-serif;
    font-size: clamp(1rem, 2vw, 1.25rem);
    font-weight: 400;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.9);
    margin: 0;
    max-width: 600px;
  }

  .sz-hero-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    margin-top: 12px;
  }

  .sz-hero-btn {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 16px 32px;
    font-family: "Inter", sans-serif;
    font-size: 15px;
    font-weight: 600;
    text-decoration: none;
    border-radius: 50px;
    transition: all 0.3s ease;
  }

  .sz-hero-btn-primary {
    background: #fff;
    color: #2C3E2D;
  }

  .sz-hero-btn-primary:hover {
    background: #D4AF37;
    color: #fff;
    transform: translateY(-2px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
  }

  .sz-hero-btn-primary svg {
    transition: transform 0.3s ease;
  }

  .sz-hero-btn-primary:hover svg {
    transform: translateX(4px);
  }

  .sz-hero-btn-secondary {
    background: transparent;
    color: #fff;
    border: 2px solid rgba(255, 255, 255, 0.5);
  }

  .sz-hero-btn-secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: #fff;
  }

  .sz-hero-trust {
    display: flex;
    flex-wrap: wrap;
    gap: 24px;
    margin-top: 24px;
    padding-top: 24px;
    border-top: 1px solid rgba(255, 255, 255, 0.2);
  }

  .sz-trust-item {
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: "Inter", sans-serif;
    font-size: 13px;
    color: rgba(255, 255, 255, 0.85);
  }

  .sz-trust-item svg {
    color: #D4AF37;
  }

  .sz-hero-scroll {
    position: absolute;
    bottom: 30px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 3;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    color: rgba(255, 255, 255, 0.7);
    font-family: "Inter", sans-serif;
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .sz-scroll-line {
    width: 1px;
    height: 40px;
    background: linear-gradient(to bottom, rgba(255,255,255,0.7), transparent);
    animation: sz-scroll-bounce 2s ease-in-out infinite;
  }

  @keyframes sz-scroll-bounce {
    0%, 100% { transform: translateY(0); opacity: 1; }
    50% { transform: translateY(10px); opacity: 0.5; }
  }

  /* Animations */
  .animate-fade-up {
    opacity: 0;
    transform: translateY(30px);
    animation: sz-fade-up 0.8s ease-out forwards;
  }

  .delay-1 { animation-delay: 0.2s; }
  .delay-2 { animation-delay: 0.4s; }
  .delay-3 { animation-delay: 0.6s; }
  .delay-4 { animation-delay: 0.8s; }

  @keyframes sz-fade-up {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .animate-fade-up {
      animation: none;
      opacity: 1;
      transform: none;
    }
    .sz-hero-image {
      animation: none;
      transform: none;
    }
    .sz-scroll-line {
      animation: none;
    }
  }

  @media (max-width: 768px) {
    .sz-hero-content {
      padding: 30px 20px;
    }

    .sz-hero-buttons {
      flex-direction: column;
      width: 100%;
    }

    .sz-hero-btn {
      width: 100%;
      justify-content: center;
    }

    .sz-hero-trust {
      flex-direction: column;
      gap: 12px;
    }

    .sz-hero-scroll {
      display: none;
    }
  }
</style>

{% schema %}
{
  "name": "Hero Banner",
  "tag": "section",
  "class": "sz-section-hero",
  "settings": [
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background Image"
    },
    {
      "type": "range",
      "id": "height",
      "label": "Desktop Height (vh)",
      "min": 50,
      "max": 100,
      "step": 5,
      "default": 85,
      "unit": "vh"
    },
    {
      "type": "range",
      "id": "mobile_height",
      "label": "Mobile Height (vh)",
      "min": 40,
      "max": 100,
      "step": 5,
      "default": 70,
      "unit": "vh"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay Opacity",
      "min": 0,
      "max": 80,
      "step": 5,
      "default": 40,
      "unit": "%"
    },
    {
      "type": "header",
      "content": "Badge"
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "Wedding Season 2024"
    },
    {
      "type": "select",
      "id": "badge_icon",
      "label": "Badge Icon",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "star", "label": "Star" },
        { "value": "heart", "label": "Heart" },
        { "value": "sparkle", "label": "Sparkle" }
      ],
      "default": "sparkle"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Personalized Bottles for Life's Special Moments"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Handcrafted with permanent sublimation printing. No vinyl, no peeling — just beautiful, lasting designs."
    },
    {
      "type": "select",
      "id": "content_position",
      "label": "Content Position",
      "options": [
        { "value": "center", "label": "Center" },
        { "value": "left", "label": "Left" }
      ],
      "default": "center"
    },
    {
      "type": "header",
      "content": "Primary Button"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "Shop Best Sellers"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "header",
      "content": "Secondary Button"
    },
    {
      "type": "text",
      "id": "button_label_2",
      "label": "Button Label",
      "default": "View Collections"
    },
    {
      "type": "url",
      "id": "button_link_2",
      "label": "Button Link"
    },
    {
      "type": "header",
      "content": "Extras"
    },
    {
      "type": "checkbox",
      "id": "show_trust_badges",
      "label": "Show Trust Badges",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_scroll_indicator",
      "label": "Show Scroll Indicator",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Hero Banner"
    }
  ]
}
{% endschema %}
